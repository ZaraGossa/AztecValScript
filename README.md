# AztecValScript

Script for validator registration with mempool analysis and priority gas setting

Must also be installed in order to use

Install Python:

```sudo apt update```
```sudo apt install python3 python3-pip -y```

Installing the necessary Python libraries:

```pip3 install web3 eth-account```


!!!during gaswars requires a significant amount of ETH on the balance sheet!!!
